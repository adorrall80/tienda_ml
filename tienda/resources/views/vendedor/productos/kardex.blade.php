<x-layouts.panel title="Kardex de {{ $producto->nombre }}"> <x-slot name="nav">@include('vendedor._nav')</x-slot> <x-slot name="actions"> <a href="{{ route('vendedor.productos.index') }}" class="btn btn-outline">Volver a productos</a> </x-slot> <div class="p-card"> <div class="p-card-header"> <h3 class="p-card-title">Historial de Movimientos de Stock</h3> </div> <div class="p-card-body p-0"> <table class="p-table"> <thead> <tr> <th>Fecha</th> <th>Usuario</th> <th>Tipo</th> <th>Referencia</th> <th style="text-align: right;">Cant.</th> <th style="text-align: right;">Balance</th> </tr> </thead> <tbody> @php $balance = 0; @endphp @forelse($movements as $m) @php $balance += $m->cantidad; @endphp <tr> <td>{{ $m->created_at->format('d/m/Y H:i') }}</td> <td>{{ $m->user ? $m->user->name : 'Sistema' }}</td> <td>{{ ucfirst(str_replace('_', ' ', $m->tipo)) }} <br><small class="text-muted">{{ $m->notas }}</small></td> <td>{{ $m->order_id ? 'Pedido #' . $m->order->numero : '-' }}</td> <td style="text-align: right; color: {{ $m->cantidad > 0 ? 'green' : 'red' }}; font-weight: bold;"> {{ $m->cantidad > 0 ? '+' : '' }}{{ $m->cantidad }} </td> <td style="text-align: right; font-weight: bold;">{{ $balance }}</td> </tr> @empty <tr> <td colspan="6" style="text-align: center;">No hay movimientos registrados.</td> </tr> @endforelse </tbody> </table> </div> </div> </x-layouts.panel>